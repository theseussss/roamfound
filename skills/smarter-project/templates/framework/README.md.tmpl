# {{ framework_name }} Framework 说明

`{{ framework_doc_path }}` 是本类 Framework 的主文件。

## 什么时候读取

- 本轮讨论改变了 {{ framework_name }} 对应的项目级稳定判断。
- 当前话题需要跨话题复用目标、结构、风格或任务链信息。
- 需要从具体 scratch/card 收束到项目级框架层。

## 怎么写入

1. 先在当前话题 `scratch.md` 记录讨论过程和最佳原话。
2. 只有当结论已经稳定、需要跨话题复用时，才维护本目录主文件。
3. 写入时保持主文件自成性：先说明对象是什么、结构如何、边界在哪里，再放误区或澄清。

## 与旧入口的关系

旧扁平入口 `{{ legacy_doc_path }}` 不再作为 Framework 主事实源；如存在，仅作为兼容索引指向 `{{ framework_doc_path }}`。
